{
    "extends": "./tsconfig.json",
    "include": ["src/**/*", "tests/**/*"],
    "exclude": [
        "**/*.config.*",
        "commitlint.config.*",
        "node_modules",
        "dist",
        "coverage",
        ".cache"
    ]
}
